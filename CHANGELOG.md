# Goose Agent changelog

Private maintenance notes are kept with the project history.

## 2026-09-24

修复
• 主窗口侧栏分隔线恢复拖动调整宽度；折叠后保留宽度，重新打开应用时沿用上次设置。
• 调整侧栏宽度时使用固定坐标系计算位移，结束拖动才保存宽度，避免分隔线来回抽动。
• 侧栏宽度变化只更新分隔区域布局，不再逐像素重新生成主窗口的弹窗和终端视图内容。

优化
• 本机终端在 PTY 尾部输出读完后，向 Ghostty 通报进程结束；应用仍能立即收到真实退出码。
• 深色终端输出直接交给 Ghostty，避免不必要的 ANSI 字节转换。
• 优先处理为空时，提示文字更轻、位置略上移，并增加与“其他会话”的间距。

## 2026-09-21

🌈 本次更新重点：英文界面更完整，跨设备操作更顺手。

✨ 新功能
• 可以复制 Goose Agent 空间位置，方便快速定位空间。

🪐 优化
• 优先会话列表切换和键盘操作更稳定。
• 移动端补齐中英文界面文案。

🐛 修复
• 用量助手只解包需要的运行文件，减少安装过程中的临时文件。
